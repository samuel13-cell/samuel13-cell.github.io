# samuel13-cell.github.io

Personal resume site. Single static HTML file, no build step, no dependencies.

- `index.html` — the page. Print stylesheet included.
- `resume.pdf` — rendered from `index.html`; served by the "Download PDF" link.

Regenerate the PDF and screenshots with `shot.py` (see the `jobapp` repo).
